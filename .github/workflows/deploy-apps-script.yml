name: Deploy Apps Script

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Install clasp
        run: npm install --global @google/clasp

      - name: Configure clasp authentication
        env:
          CLASPRC_JSON: ${{ secrets.CLASPRC_JSON }}
        run: |
          test -n "$CLASPRC_JSON" || { echo "Missing CLASPRC_JSON secret"; exit 1; }
          printf '%s' "$CLASPRC_JSON" > "$HOME/.clasprc.json"
          chmod 600 "$HOME/.clasprc.json"

      - name: Push Apps Script
        working-directory: automation/apps-script
        run: |
          set -o pipefail
          clasp push --force | tee /tmp/clasp-push.log
          if grep -q "Skipping push" /tmp/clasp-push.log; then
            echo "::error::clasp push was skipped instead of pushing — the deploy did not update the live Apps Script project." >&2
            exit 1
          fi
